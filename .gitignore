# 本地开发 Secrets，绝不提交
.dev.vars
.env
.env.*
!.env.example

# 本地 wrangler 状态 / 本地 KV 持久化
.wrangler/

# 构建产物（可选，若不想提交）
_worker.js

# 烘焙配置产物（方案 A：静态部署 / 完全不依赖 KV）。
# 由「管理后台导出配置」得到的镜像经构建脚本生成，含部署专属配置，不纳入版本管理。
src/config/baked.generated.js

# 构建期自动生成的文件（由 build.mjs 生成，勿手动编辑/提交）
src/ui.gen.js
web/_stage.gen.js
web/_app.entry.js
web/_stage.entry.js
dist/
# EO Makers 仅上传产物部署包（由 scripts/package-eo.mjs 本地构建生成）
dist-eo/

# 文档站构建产物（MkDocs 生成，不纳入版本管理）
docs-site/site/

# node
node_modules/
npm-debug.log*
.npm/

# 本地参考资料（第三方脚本，含硬编码凭据，不纳入版本管理）
refs/

# 系统 / 编辑器
.Trash-0
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/*
!.vscode/settings.json
.codebuddy/plans/*
# 本地克隆的外部演示仓库，不纳入版本控制
next-cnb-eo-demo/
