{
  // cdn-edge-gateway — 阿里云 ESA Functions / Pages 部署配置
  //
  // 基础设施即代码（IaC）：本文件优先级高于 ESA 控制台同名配置。
  // 文档依据：阿里云 ESA「PAGES构建和路由指南」。
  //
  // 关键约定：
  // 1. entry            → ESA 函数入口（薄壳，转发到构建产物 _worker.js）
  // 2. assets.directory → 静态资源目录（复用 build.mjs 产出的 dist/public）
  // 3. 不配置 notFoundStrategy：
  //    ESA 文档明确「若同时配置函数脚本与 notFoundStrategy，导航请求不会触发
  //    函数脚本」。本项目管理面 /__panel 是导航请求，若配 singlePageApplication
  //    会被 index.html 兜底吃掉、永远进不了函数。故不配置 notFoundStrategy，
  //    让所有未命中静态资源的请求（含 /__panel 导航）都落到函数执行。
  //    —— 备选：若实测 ESA 默认把导航请求兜底 index.html，则改为
  //       "notFoundStrategy": "404Page" 且仓库不提供 404.html，使其落到函数。
  // 4. 函数代码包 ≤ 4MB、静态单文件 ≤ 25MB（本项目远小于此）。
  // 5. 运行限制：内存 128MB、执行时间 120s。
  //    每请求子请求(fetch)预算：官方文档两处表述冲突且未说明关系——
  //      · fetchAPI：「目前每次可以发起 4 个子请求；4 个及以上需申请配额」
  //      · Cache API：「Cache 操作与 fetch 共享 32 个子请求约束」
  //    → 本项目保守取较小值 4（caps.js: maxSubRequests=cacheSubreqLimit=4），待真机实测。
  //    数据面稳态≈2 个 fetch（1 回源 + 至多 1 静态同站），在 4 内安全。
  // 6. 环境变量在【ESA 控制台】设置（esa.jsonc 不支持顶层 env 字段），包括：
  //      CLOUD_PLATFORM=esa          —— 显式声明平台（规范值），caps.js 据此探测
  //      REDIS_URL                   —— 【强烈建议配置】外置持久化（自建 Webdis/Redis）。
  //                                      ESA 的 EdgeKV 按量收费且无免费额度，本项目在 ESA
  //                                      上统一禁用厂商 KV，可写持久化只能走 REDIS_URL。
  //                                      配上后 esa/index.js 会【自动退出静态烘焙只读模式】，
  //                                      管理面即可正常保存配置（与 CF / EO 行为一致）。
  //                                      不配则默认回退静态烘焙（只读，见第 7 条）。
  //                                      格式：https://webdis.example.com（不带尾斜杠）
  //      REDIS_TOKEN                 —— 可选，Webdis 鉴权头原样值，如 Basic <base64>
  //      REDIS_PREFIX                —— 可选，键前缀（多项目共库时便于识别）
  //      REDIS_DB                    —— 可选，Redis 逻辑库号 0–15，非法值回退 0
  //      REDIS_TIMEOUT_MS            —— 可选，Webdis 单次请求超时，默认 5000
  //      KV_BACKEND                  —— 可选，KV 后端选型：auto（默认，Webdis 优先）/
  //                                      native（平台 KV）/ redis（Webdis）。
  //                                      ESA 无可用平台 KV，故此处通常无需设置。
  //                                      ⚠️ 只能走环境变量：配置本身存在 KV 里，写进配置
  //                                      会形成「读配置前需先知道用哪个后端」的循环依赖。
  //      ADMIN_PASSWORD / JWT_SECRET —— 可选，凭据类变量
  // 7. STATIC_CONFIG（静态烘焙只读模式）—— 由 esa/index.js 按 REDIS_URL 自动决定：
  //      配了 REDIS_URL 且未显式设置  → 不烘焙，走 Webdis 可写模式
  //      未配 REDIS_URL 且未显式设置  → 默认 STATIC_CONFIG=1（只读兜底，读构建时内置配置）
  //      显式 STATIC_CONFIG=1        → 强制只读烘焙（即使配了 REDIS_URL）
  //      显式 STATIC_CONFIG=0        → 强制可写（无 REDIS_URL 时配置无法保存）
  // 8. 实时日志：ESA 官方《RuntimeAPI手册》明确同时支持 console.log() 与 console.alert()；
  //    log 用于控制台调试环境 debug 打印，alert 用于输出关键信息至日志。本项目无需
  //    将 log 重定向到 alert（esa/index.js 仅在 console.log 缺失时才兜底到 alert）。

  "name": "cdn-edge-gateway",
  "entry": "./esa/index.js",
  "installCommand": "npm ci",
  "buildCommand": "npm run build",
  "assets": {
    "directory": "./dist/public"
  }
}
