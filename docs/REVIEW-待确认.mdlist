# 待确认问题列表（REVIEW mdlist · 重新审查）

> 生成时间：2026-08-11 21:51
> 审查方式：**4 名审查员分模块独立审查（只读，未改任何代码）+ 主审复核关键产物**。
> 分项：A. 构建产物未同步（最高优先级，横跨代码层+构建层）；B. 代码层 bug / 逻辑；C. 文档一致性；D. 流量序列 UI（源文件层 vs 产物层）。
> 每条标注严重级（高/中/低）、类型、方向（代码领先 / 文档领先 / 都错 / —）、是否需确认。
> 是否修改、如何修改，留待后续讨论决定。

---

## ⚠️ 审查前置结论（最重要）

**经独立核查，上一份 REVIEW-待确认.mdlist 中大量"✅ 已修复 / 构建已重新生成"的声明，在「产物层面」并不成立。**

证据（用 `python3` 对产物文件逐符号统计，结果确凿）：
- `web/app.js`（源）含流量序列核心函数：`renderRuleStage`×8、`seqStage`×28、`openCacheGenDrawer`×2、`wireRuleDrag`×2、`confined`×7、`openRulesDrawer`×3、"流量序列"文案×23。
- `src/ui.gen.js`（内联兜底产物）这些符号**全部为 0**；`dist/public/assets/app.js`（静态托管产物）这些符号**全部为 0**；两者连"流量序列""站点管理""拖拽"等字符串文案都是 0。
- 但 `src/ui.gen.js` 的 HTML 外壳含"流量序列"导航项（说明 index.html 当时已是新版），而 `build.mjs` 的 `buildInlineUI` 逻辑本身正确（正则能匹配并移除 `<script src="api.js/app.js">` 并注入拼接后的 `frontendJs`）。

**结论**：`web/`（app.js/index.html/api.js）在加入流量序列 UI 后，**即使重新 `npm run build`，产物仍缺失管理面 UI 渲染逻辑**——根因是 `build.mjs` 的 esbuild 压缩（`minify:true`）把 `web/app.js` 中通过对象属性/事件**间接注册**的 UI 函数（流量序列、站点管理等）当作不可达死代码消除（详见 A1 二次审查）。REVIEW.mdlist 把"产物无流量序列"归因为"没重新 build"是**错误判断**；真实是 **build 脚本缺陷导致任何构建都产出残缺管理面**。

---

## A、构建产物未同步（最高优先级）

### 🟢 A1. `src/ui.gen.js` 与 `dist/public/assets/app.js` 均未包含流量序列/管理面 UI，缺失重大功能（高·构建层 bug / 产物过期）—— **已修复（状态1 / 方案 Y）**

- 位置：`build.mjs:111-132`（`buildFrontendJs`，esbuild 压缩 `api.js+app.js`）；产物 `src/ui.gen.js`（`UI_JS`）、`dist/public/assets/app.js`；源 `web/app.js`。
- **二次全面审查复核（2026-08-11 22:25，确认问题真实存在，并纠正原"未重新 build"误判）**：
  1. 符号统计：改动前 `src/ui.gen.js` / `dist/public/assets/app.js` 的 `renderRuleStage`/`seqStage`/`openCacheGenDrawer`/`wireRuleDrag`/`confined`/`openRulesDrawer` 全部为 0，连"流量序列""站点管理"文案也为 0；而 `web/app.js` 含完整实现（renderRuleStage×8、seqStage×28 等）。→ **问题确实存在**。
  2. 实际运行 `npm run build`：产物**仍然**不含这些符号（UI_JS 仅 242 字符，内容只是 `api.js` 的 `ApiError` 类 + `et()`，app.js 逻辑全无）。→ **推翻"根因是没跑 build"**：跑 build 也照样坏，说明是 build 脚本本身的缺陷。
  3. 复现 esbuild：`api.js+'\n'+app.js`(153KB) 经 esbuild（`bundle:false, minify:true, format:'iife'`）压缩后输出 139KB，**`renderRuleStage` 为 false**；**单独压缩 `web/app.js`** 输出 135KB，`renderRuleStage` 仍为 false；`treeShaking:false` 也仍 false；但 `minify:false` 时 `renderRuleStage` 为 **true**。→ **根因锁定在 esbuild 的 minify 死代码消除**。
  4. 最小复现：顶层定义 `function foo(){};function bar(){return foo()}` 仅通过 `document.addEventListener` 间接引用，esbuild `minify:true` 时 `foo/bar` 被移除，`minify:false` 时保留。→ 确认 esbuild minifier 会丢弃"未被入口可达链直接引用"的函数。
- **真实根因**：`web/app.js` 大量 UI 函数（含流量序列 `renderTrafficSequence`/`renderRuleStage`/`seqStage` 及 `renderSites`/`renderPools` 等）并非被底部启动函数直接文本调用，而是通过**对象属性/事件间接注册**（如 `web/app.js:229` 的 `sites: renderSites` 挂到 view 映射对象）。esbuild 在 `minify:true` 下把"间接引用"判定为不可达死代码并移除；`login`/`addEventListener`/`je()` 等因被文件底部启动函数直接调用而保留。原 REVIEW.mdlist 把"产物无流量序列"归因为"没重新 build"是**错误**的——**build 脚本配置缺陷会导致任何一次构建都产出残缺管理面**。
- 后果（视部署形态）：走 `dist/public` 静态托管（CF Pages/Workers ASSETS/EO Pages）或 `src/ui.gen.js` 兜底（CF Workers 粘贴）的线上管理面，**均无流量序列、无站点管理渲染逻辑**，登录后主界面/流量序列页为空或残缺。
- 类型：构建层 bug（esbuild minify 死代码消除误杀间接引用函数）
- 方向：—
- 是否需确认：**是（P0）**，根因已确凿，修复方案已实测对比：
  - **方案 X（实测 ❌ 不可靠）**：在 `web/app.js` 末尾把核心 UI 函数挂到 `window.__ui` / `void [...]` 显式引用，意图使 esbuild minify 视为被引用而保留。**实测失败**：即便加了 `window.__ui = {...}` 与 `void __uiFns` 数组引用，压缩产物 `app.js` 仍仅 **139 KB**，`renderTrafficSequence`/`renderSites`/`openRulesDrawer` 虽留"引用壳"（`renderTrafficSequence:Xe`），但**函数体 `Xe` 定义与所有中文文案（"流量序列"×0、"站点管理"×0）被消除**，UI 仍残缺。根因：esbuild minify 做跨过程副作用分析，`window.__ui` 赋值与 `void` 数组均无"读取侧"，整条引用链被判无副作用删除。**结论：在 `bundle:false + minify:true` 下纯挂 window 无法保活动态键访问的间接函数，方案 X 不可靠，不推荐。**
  - **方案 Y（实测 ✅ 彻底可用）**：`buildFrontendJs` 对 web 资源改用 `minify:false`（仅管理面兜底），避免死代码消除；Worker 仍走全局 `MINIFY` 压缩。实测（2026-08-11 22:40）：
    - 默认压缩构建下 `dist/public/assets/app.js` = **187 KB**（含完整 UI：renderRuleStage×8 / seqStage×28 / renderTrafficSequence×2 / renderSites×2 / openRulesDrawer×3 / confined×7；文案"流量序列"×15、"站点管理"×4）；`src/ui.gen.js` 文件 446 KB（内联 JSON 转义开销，UI 完整）；`_worker.js` = **558 KB**（仍压缩，远离 CF 1MB 限制）。
    - 对照「全量 `--no-minify`」：`app.js` 同为 187 KB、`_worker.js` 690 KB、`ui.gen.js` 471 KB；对照「原压缩误杀」：`app.js` 仅 139 KB 且 UI 全缺；对照「方案 X 实测」：`app.js` 139 KB 但 UI 仍缺（X 无效）。
    - **结论**：方案 Y 修复彻底、零源码改动、Worker 体积不受影响；代价仅是管理面静态 JS 由 139KB→187KB（+48KB），以及 `ui.gen.js` 兜底文件由残缺→446KB，**均在 CF 1MB 内，安全**。
  - **方案 Z（改构建形态）**：用 esbuild `bundle:true` + 显式 entry 打包 `web/`，但 `app.js` 非标准 ESM 模块，需评估副作用。
  - **下一步决策：推荐采用方案 Y**（零源码侵入、已验证恢复完整 UI 且代价极小）。方案 X 实测不可靠已排除；若坚持要保留压缩形态，需改用更激进的 X 变体（例如 `ROUTES`/`TITLES` 改为被启动链直接调用一次）或方案 Z，待讨论。
- **方案 Y 修复后二次全面复核（2026-08-11 22:54，确认 A1 已全面解决）**：基于暂定状态1（默认 `npm run build`，前端 UI 不压缩、Worker 仍压缩）重新构建，对产物逐符号+文案统计 + 自检：
  - 静态托管 `dist/public/assets/app.js`（149.9 KB）：全部 UI 符号在场 —— `renderRuleStage`×8、`seqStage`×28、`wireRuleDrag`×2、`renderSite`×5、`confined`×7、`seqRuleInPack`×3、`seqGroup`×18、`gStage`×8；路由/抽屉函数 `renderOverview/renderSites/renderTrafficSequence/renderPools/renderCache/renderSystem` 各 2-3、`openRulesDrawer`×3 等全部在场；文案「流量序列」×15、「站点管理」×4、「规则引擎」×12、「拖拽」×5、「源站池」×38 均在。**整条流量序列渲染链完整保留，无"引用壳但函数体缺失"**。
  - 内联兜底 `src/ui.gen.js`（369.2 KB）：`UI_JS` 含 `renderTrafficSequence`×4、`renderRuleStage`×16、`seqStage`×56、`renderSites`×4、`openRulesDrawer`×6、`confined`×14；文案「流量序列」×31、「站点管理」×9 → **CF Workers 粘贴兜底形态 UI 也完整**。
  - `_worker.js`（486.2 KB）：仍压缩形态（换行极少）、含 `onRequest` 导出，`build.mjs` 自检（`verify()` 真正 `import` 加载）通过 → **数据面/入口未受影响，远离 CF 1MB 限制**。
  - 对照原误杀态（app.js 仅 139KB、UI 全缺）与方案 X 实测态（app.js 139KB、UI 仍缺）：方案 Y 产物 app.js 149.9KB 且符号/文案全在，确证 A1 **全面解决**（静态托管与兜底两条部署链路的管理面均恢复）。
- 关联：D 类（U1-U5 源文件层已正确）的线上生效**已随 A1（方案 Y）修复而打通**——静态托管与 `ui.gen.js` 兜底两种部署形态现在都能加载完整流量序列/管理面 UI。A1 关闭后，D 类仅余"源文件层是否还有可优化项"的常规审查，不再阻塞线上。

---

## B、代码层问题（bug / 空挂 / 逻辑错乱 / 冗杂）

> 说明：数据面核心（failover 的 originUrl、resolveHostHeader 三参、accel 分支）经审查员 A 独立核查，**已正确**，原 REVIEW.mdlist 的 C1/C2 修复在源文件层属实，此处不再重复列为 bug。

### 🟢 B1. `pools.js` `collectRefs` 在 `getGlobalRules` 失败时未置 `truncated=true`，可能低估引用导致误删源站（中·逻辑空挂 / fail-open 安全漏洞）—— **已修复**

- 位置：`src/api/handlers/pools.js:63-79`（`collectRefs` 内 `try { getGlobalRules } catch { ... }`）。
- 现象：`getGlobalRules` 异常被吞，仅注释"引用信息不完整"，但 `truncated` 保持原值（来自 `listAllSites`）。前端据此认为引用完整，`remove` 时 `refs.length===0` 会**误删仍被全局规则引用的源站池** → 全站 502。
- 推断根因：`catch` 分支未保守置 `truncated = true` 阻断删除。
- 类型：逻辑空挂 / 潜在数据面故障
- 方向：—
- **二次全面复核（2026-08-11 23:01，确认问题真实存在，非误判）**：
  1. 实读 `src/api/handlers/pools.js:27-82`：`collectRefs` 第 37 行 `truncated` 来自 `listAllSites(ctx)`，第 63-76 行 `try { const globalRules = await getGlobalRules(ctx); ... }`，第 77-79 行 `catch {}` **为空**，未改 `truncated`。
  2. 实读 `src/config/store.js`：第 452-469 行 `listAllSites` 的 `truncated` **仅表示"站点列表是否扫全"**（超过 `MAX_TOTAL_SITES_SCAN` 翻页上限才置 true），**与全局规则无关**；第 581-585 行 `getGlobalRules` 内部 `readJson(ctx, K_GLOBAL_RULES)` 可能因 KV 读取/解析异常**抛错**（仅"成功但格式不对"才返回 `[]`）。
  3. 误删路径成立：若某源站池 P **仅被全局规则引用**（无任何站点直接引用，`listAllSites` 的 `truncated=false`），且 `getGlobalRules` 抛错被静默 → `collectRefs` 返回 `truncated=false` 且 `map` 中 P 无引用记录 → `remove(P)` 走第 172-183 行：`refs.length===0 && !truncated` → `deletable=true`、放行删除（第 194 行）→ P 被删但全局规则仍指向 P → 命中该全局规则的请求回源到已删池 → **全站 502**。此为 fail-open 安全漏洞。
  4. 结论：**B1 确凿存在，非误判**，属 fail-open。修复点明确且最小：`catch` 块内 `truncated = true`（表示"全局规则引用信息不完整，不可判定无引用"），使 `remove` 走第 177 行保守拒绝（409），与站点维度 `truncated` 的语义一致。
- **修复（2026-08-11 23:03）**：`src/api/handlers/pools.js`
  - 第 37 行 `const { sites, truncated }` → `let { sites, truncated }`（使 catch 可重赋值）。
  - 第 77-79 行 `catch {}` 改为 `catch { truncated = true; }` 并补注释说明 fail-open 风险。
  - **注意**：二次验证时一度暴露本修复自身的笔误——初版直接 `truncated = true` 但变量仍是 `const`，运行时抛 `TypeError: Assignment to constant variable`，经端到端测试发现后已改正为 `let`。这正说明"先确认再动手 + 修复后二次验证"流程的价值。
- **修复后二次验证（端到端，2026-08-11 23:05）**：用 ESM loader 拦截 `store.js`，构造「站点维度 `truncated:false` + `getGlobalRules` 抛错」的误删前置条件，分别跑旧版与修复版 `remove(ctx,'P')`：
  - 修复前（旧版）：`HTTP 200` + `deletePool called: true` → **误删发生（fail-open），确认漏洞真实**。
  - 修复后（当前）：`HTTP 409` + `error.code: CONFLICT`（"站点数量过多，无法完成引用检查…已阻止"）+ `deletePool called: false` → **误删路径已堵住，保守拒绝**。对照通过，B1 全面解决。

### ⚪ B2. `signedUrl` 开启但 `secret` 为空 → 全站 403（中·可用性风险，已文档化）—— **误判 / 跳过（无需修复）**

- 位置：`src/security/guard.js:285-323`（`signedUrlBlocked`）、`contracts.js`/`schema.js` 对 `signedUrl.secret` 的校验。
- 现象（原担忧）：`guard.js:289` 规定"未配置密钥时无法校验；此时拦截而不是放行"。即 `signedUrl.enabled=true` 且 `secret` 为空 → 所有请求 403，站点自锁。属 fail-closed 正确安全行为；文档（08/11/12/04/05）已统一标注"⚠️ 实验特性（签发工具待开发）"，前端 `web/app.js` 也有提示。
- **二次全面复核（2026-08-11 23:15，确认属误判，非真正待改项）**：原 B2 的核心待确认点——"schema 是否强制 `signedUrl.secret` 非空"——经实读源码确认**已被满足**：
  1. `src/config/schema.js:530-534`：`const signedEnabled = bool(su.enabled, ...); const signedSecret = str(su.secret, '', 512); if (signedEnabled && !signedSecret) { errors.push('启用签名 URL 时必须设置 secret'); }` —— **配置校验阶段已强制 secret 非空**。
  2. `src/config/schema.js:754-757`：`normSecurity` 的 `errors` 被 push 到 `c.errors`，只要 `c.errors.length` 即 `return { ok:false, errors }`，**配置保存被拒绝，不会落库**。
  3. `grep` 确认 `normSecurity` 调用方唯一（仅 schema.js:754 经 `normSite`），且 `contracts.js:272` 仅含 typedef 类型描述、**无独立运行时校验**，校验完全集中在 schema 层，无旁路写入入口绕过该校验。
  4. `src/config/defaults.js:269-274`：`DEFAULT_SIGNED_URL = { enabled:false, secret:'' }`，默认关闭；不存在"默认开启空密钥"的出厂残态。
- **结论**：`schema.js` 已在**配置保存层**堵死"开启签名却留空 secret"的写入路径，`guard.js:289` 的 fail-closed 拦截只是**纵深防御**（即便配置被旁路写入也安全），**不构成可用性 bug**。`signedUrl` 全站 403 自锁的担忧路径在正确配置流程下不可达。B2 **属误判，无需代码改动**，标记为跳过。
- 类型：原判"可用性风险 / 输入校验缺口" → 复核后证实校验已存在，缺口不成立。
- 方向：—
- 是否需确认：**否（已复核闭环）**，无需修复。

### 🟢 B3. `ratelimit.js` `resetGlobalRateLimit`/`resetRateLimitMemory` 死代码（低·冗杂）

- 位置：`src/security/ratelimit.js:234-237`、`:288-290`。
- 现象：全项目零调用（`checkGlobalRateLimit` 仅被 pipeline 调用，且 `globalRateLimit` 默认 0、前端标"实验特性"）。
- 类型：死代码 / 逻辑冗杂
- 方向：—
- 是否需确认：否，可安全删除或补测试。

### 🟢 B4. `LOGIN_GUARD_LIMITS` / `COLLECTOR_LIMITS` 导出常量前端未接线（低·冗杂）

- 位置：`src/security/loginGuard.js:282-286`、`src/stats/collector.js:431-434`。
- 现象：`Object.freeze` 导出但无模块消费；前端 `web/app.js` 未引用，也未被 `system/info` 返回。属"后端常量就绪、前端未应用"。
- 类型：未使用的公共 API
- 方向：—
- 是否需确认：否，可保留（删除会丢失可复用阈值常量）。

### 🟢 B5. `hourKey(ts)` 在 kvDriver/d1Driver/index 三处重复实现（低·冗杂）

- 位置：`src/stats/kvDriver.js:76-83`、`src/stats/d1Driver.js:122-130`、`src/stats/index.js:99-107`。
- 现象：逻辑完全相同（UTC `yyyymmddhh`），重复三份。
- 类型：逻辑冗杂
- 方向：—
- 是否需确认：否，建议抽公共工具。

### 🟢 B6. `web/app.js` `openInitialOriginDrawer` 残留（低·轻微冗余，无回归）

- 位置：`web/app.js:2052`（仍在，但仅作 ⑭ 无源站兜底入口，不再被 ③ 调用）。
- 现象：U5 修复后 ③ 已收敛为只读 toast，该函数已不被错误路径调用，仅作兜底。属轻微冗余。
- 类型：轻微冗余
- 方向：—
- 是否需确认：否，可保留或清理。

### 🟢 B7. `guard.js` IPv6 CIDR 注释与实现不符（低·注释误导，无功能 bug）

- 位置：`src/security/guard.js:97-100`、`106-109`。
- 现象：注释写"IPv6 CIDR 不支持，退化为字符串前缀判断"，实际代码是 IPv6 CIDR 直接 `return false`（不命中），仅 `*` 通配支持 IPv6。行为正确（不会误放行），仅注释措辞偏差。
- 类型：注释与实现不符
- 方向：—
- 是否需确认：否。

---

## C、文档一致性问题（mdlist）

> 方向标注：**代码领先**（md 落后）/ **文档领先**（md 领先/代码落后）/ **都错**。

### 🟢 C1. `docs/04-configuration.md` match `target` 枚举漏列 `origin`（中·代码领先）—— **已修复**

- 文档位置：`docs/04-configuration.md:196`（列 15 个值，缺 `origin`）。
- 代码真相：`src/config/defaults.js:189-193` 的 `MATCH_TARGETS` 含 `origin`；`web/app.js:954` 有 `{value:'origin', label:'回源目标（源站 ID…）'}`。
- 差异：`origin` 是"③ 首要分流后按负载均衡选出的回源对象"维度，前端与 schema 都支持，文档枚举漏列。
- 方向：代码领先（文档落后）
- 是否需确认：否，补 `origin` 即可。
- **二次全面复核（2026-08-11 23:19，确认问题真实存在，非误判）**：
  1. 实读 `src/config/defaults.js:189-193`：`MATCH_TARGETS` 冻结数组共 **16** 个值，末尾为 `'origin'`；`docs/04-configuration.md:196` 原文列 15 个（至 `referer` 结束），**确无 `origin`** → 漏列属实，非误判。
  2. 交叉佐证：`web/app.js:955` 前端下拉确有 `{value:'origin', label:'回源目标（源站 ID，由 ③ 首要分流按负载均衡选出）'}`；`src/config/schema.js:351` `enumOf(c.target, MATCH_TARGETS, '')` 接受 `origin` → 代码层 `origin` 维度已全链路接通，仅文档落后。
- **修复（2026-08-11 23:19）**：`docs/04-configuration.md:196` 在 `referer` 后补 ` / \`origin\``，枚举由 15 → 16 项，与 `MATCH_TARGETS` 逐一对齐；原 15 项顺序/名称不变。
- **修复后二次验证（静态逐符号比对）**：修复后文档枚举 = `host,path,fullUrl,query,extension,filename,directory,method,protocol,header,cookie,clientIp,clientCountry,userAgent,referer,origin`（16 项），与 `MATCH_TARGETS` 完全相等；文档漏列项 = 无，文档多余项 = 无 → C1 **全面解决**（代码领先→文档同步）。

### 🟢 C2. `docs/11-architecture.md` 目录树引用不存在的 `proxy/image.js`（中·文档领先）—— **已修复**

- 文档位置：`docs/11-architecture.md:49`（`proxy/image.js # 图片优化`）、流程描述 `proxy.image()`（原行 108）。
- **二次全面复核（2026-08-11 23:23，确认问题真实存在，非误判，并纠正原 mdlist 摘要两处误述）**：
  1. 实读 `docs/11-architecture.md:48-49`：目录树确列 `└── image.js # 图片优化（Accept 协商 webp/avif，仅 CF）`。`search_file` 在 `src/proxy/` 递归搜 `image.js` 返回 **0 文件**；`src/` 下搜 `*image*.js` 也 **0 文件** → **该模块确不存在**。
  2. 实读 `docs/11-architecture.md:106`（原行号）：流程描述 `├─ proxy.image()：图片优化（CF 协商 webp/avif；EO 降级原图）`。`grep` 在 `src/` 全仓搜 `.image(` 与 `imageOptimization` **0 命中** → **`proxy.image()` 调用在代码里也不存在**；图片优化属 CF 平台原生 `fetch` 协商能力，无独立 JS 模块。
  3. **纠正原 mdlist 摘要误述**：① 摘要称"第 104、151 行亦引用 image.js" 不准确——行 104 实为 `fallbackStatusCodes`（属 C5）、行 151 是能力对比表"图片优化(webp/avif)"功能行（非路径引用）；真实第二处引用是流程描述的 `proxy.image()`（原行 108）。② 摘要称"图片优化逻辑在 `src/proxy/headers.js` 等内" 不成立——`src/proxy/` 内无任何 webp/avif 逻辑，图片优化依赖平台原生能力。C2 实为**文档虚构模块 + 虚构调用**，非"逻辑移了位置"。
- 方向：文档领先（代码无该文件/该调用）
- 是否需确认：否。
- **修复（2026-08-11 23:23）**：`docs/11-architecture.md`
  - 目录树：删除 `└── image.js # 图片优化` 行，并将上一行 `cache.js` 的 `├──` 改为 `└──`（变为末节点），保持树形正确。
  - 流程描述：将虚构的 `proxy.image()：图片优化（CF 协商 webp/avif；EO 降级原图）` 改为如实描述「图片优化：依赖 CF 平台原生 fetch 协商 webp/avif（**无独立 JS 模块**，`src/proxy/` 下不存在 image.js）；EO 因无该能力自动降级为原图」。
- **修复后二次验证**：`search_content` 在 `docs/11-architecture.md` 再搜 `proxy/image.js` / `proxy.image()` → **0 命中**；`search_file` 在 `src/proxy/` 搜 `image.js` 仍 0 文件。文档已不再引用不存在的模块/调用，C2 **全面解决**（文档与代码现状对齐：图片优化为平台能力，无独立源文件）。

### 🟢 C3. `docs/04-configuration.md:71` 误称 `wrangler.toml` 已带 `sockets` 兼容标志（中·文档与 toml 矛盾）—— **已修复**

- 文档位置：`docs/04-configuration.md:72/74`（说明段："CF 端自定义回源 Host…必须用 Workers 形态部署（`wrangler deploy`，`wrangler.toml` 已带 `sockets` 兼容标志）…"）。
- **二次全面复核（2026-08-11 23:27，确认问题真实存在，非误判）**：
  1. 实读 `docs/04-configuration.md:74`：原文确写"`wrangler.toml` 已带 `sockets` 兼容标志"。
  2. 实读 `wrangler.toml:14-18`：注释"**不要在此加 "sockets"** —— workerd 无此 flag，会导致 wrangler dev / deploy 直接启动失败（No such compatibility flag: sockets）"；`compatibility_flags = ["nodejs_compat"]`（**无 sockets**）。
  3. 代码真相：`src/proxy/engines/socketEngine.js:95` 运行时 `import("cloudflare:sockets")`，`build.mjs:281` 显式将其列为 esbuild 虚拟模块；多份文档（08-faq:28、09-local-dev:58、03-deploy:232）均确认"**不需要**声明 sockets flag"。→ 文档"已带 sockets 兼容标志"与 toml 及代码现实**直接矛盾**，属错误描述。
- 方向：文档领先（错误描述）
- 是否需确认：否。
- **修复（2026-08-11 23:27）**：`docs/04-configuration.md:74` 删除"`wrangler.toml` 已带 `sockets` 兼容标志"，改为"**`cloudflare:sockets` 由 `socketEngine.js` 运行时动态 `import`，无需在 `wrangler.toml` 声明 `sockets` 兼容标志（`compatibility_flags` 仅有 `nodejs_compat`；加 `sockets` 反而会因 workerd 无此 flag 导致启动失败）**"。
- **修复后二次验证**：`search_content` 在 `docs/04-configuration.md` 再搜"已带 sockets 兼容标志" → **0 命中**；`grep wrangler.toml` 确认 `compatibility_flags = ["nodejs_compat"]` 且 toml 注释明确禁加 sockets，与修复后文档一致 → C3 **全面解决**（文档与 toml/代码现实对齐）。

### 🟢 C4. `docs/08-faq.md:128` 规则匹配仍列旧名 `asn/country/scheme`（中·文档内部矛盾/都错）—— **已修复**

- 文档位置：`docs/08-faq.md:128`（"可匹配 host/path/…/userAgent/asn/country/scheme 等"）。
- 代码真相：`asn/country/scheme` 正是 `docs/04-configuration.md:202` 明确宣布**已废弃**的旧名；真实枚举见 `defaults.js:189-199`（`clientCountry`/`clientIp` 等）。
- **二次全面复核（2026-08-11 23:35，确认问题真实存在，非误判）**：
  1. 实读 `docs/08-faq.md:128`：原文在 `match.conditions` 可匹配维度语境下列 `…/userAgent/asn/country/scheme 等`，三者均为 `target` 旧名。
  2. 实读 `docs/04-configuration.md:200`：明确"`asn`/`country`/`scheme` 不再是 `target` 枚举（改为 `clientCountry`/`clientIp` 等）"，与 08 矛盾 → 文档内部不一致成立。
  3. 代码真相交叉核验：`grep` 在 `src/` 全仓搜 `target==='asn'/'country'` 及 `asn`/`country` 作为 target 引用 = **0 命中**；`src/config/defaults.js:189-193` 的 `MATCH_TARGETS` 含 `protocol`/`clientIp`/`clientCountry`、**不含** `asn`/`country`/`scheme`（作为 target）；`src/proxy/matcher.js:64-65` 实际提取 `protocol`/`clientIp`，`src/contracts.js:158` typedef 亦为 `'protocol'|'header'|'cookie'|'clientIp'|'clientCountry'`。→ 旧名代码层已彻底移除，文档列旧名确属错误。
  4. 注意 `scheme` 在代码里有**另一活跃含义**：源站回源协议字段 `action.scheme`（04:238、09:152 等），与 `target` 维度无关；故仅作为 target 旧名删除，不影响回源协议 `scheme` 字段的正确性。
- 方向：都错（08 用旧名，04 已废弃）
- 是否需确认：否。
- **修复（2026-08-11 23:35）**：`docs/08-faq.md:128` 将匹配维度列表中的 `asn/country/scheme` 删除，改为现行维度 `clientCountry/clientIp/protocol`，并补注"旧名 `asn`/`country`/`scheme` 已废弃，对应现行维度为 `clientCountry`/`clientIp`/`protocol`"，与 04 枚举及代码现实对齐。
- **修复后二次验证**：`search_content` 在 `docs/08-faq.md` 再搜 `asn/country/scheme` / `userAgent/asn` → **0 命中**（仅 mdlist 讨论文字残留）；修复后 08:128 列 `clientIp/clientCountry/referer/userAgent/protocol`，与 `MATCH_TARGETS` 现行枚举一致；代码层 `asn`/`country` target 引用仍 0 → C4 **全面解决**（08 与 04/代码对齐，旧名清除）。

### 🟢 C5. `fallbackStatusCodes` 旧字段名残留于多篇文档（中·代码领先）—— **已修复**

- 文档位置：`docs/11-architecture.md:53,104,235`、`docs/08-faq.md:134`（均写 `fallbackStatusCodes` 如 502/503）。
- 代码真相：`src/` 全仓无 `fallbackStatusCodes` 字段；实际为 `contracts.js:429` 的 `DEFAULT_RETRY_ON` / schema 的 `retryOn`。
- **二次全面复核（2026-08-11 23:40，确认问题真实存在，非误判）**：
  1. `grep -rn "fallbackStatusCodes" src` → **0 命中**，代码层确无此字段。
  2. 代码真相交叉核验：`src/balancer/failover.js:8,37-40,147-148` 使用 `failover.retryOn`（默认 `DEFAULT_RETRY_ON`）；`src/config/defaults.js:324` `retryOn: DEFAULT_RETRY_ON`；`src/config/schema.js:957-972` 归一 `retryOn`；`src/contracts.js:326` `@property {number[]} retryOn 触发换源的状态码，默认 [500,502,503,504,522,524]`。→ 现行字段名确为 `retryOn`。
  3. 文档侧 5 处残留：`11-architecture.md:52/103/234`、`08-faq.md:134`（mdlist 摘要行号 53/104/235 为旧偏移，实际已随 C2/C3 编辑上移，但内容点一致）→ 旧名残留属实，属代码领先（文档落后）。
- 方向：代码领先（旧名残留）
- 是否需确认：否。
- **修复（2026-08-11 23:40）**：将 `docs/11-architecture.md`（3 处）与 `docs/08-faq.md`（1 处）的 `fallbackStatusCodes` 全部替换为 `retryOn`（保留"如 502/503"示例，因 `retryOn` 默认含 502/503，语义一致）：目录树注释 `链式回退（按 order + retryOn 状态码）`、流程 `失败（网络错误 / retryOn 状态码）→ 链式回退`、能力说明 `源站返回 retryOn 状态码（如 502/503）`、FAQ `源站返回 retryOn 状态码（如 502/503）`。
- **修复后二次验证**：`grep -rn "fallbackStatusCodes" docs/11-architecture.md docs/08-faq.md docs/04-configuration.md docs/12-request-flow.md docs/05-user-guide.md docs/03-deploy.md docs/09-local-development.md docs/10-api-reference.md` → **0 命中**（仅 mdlist 自身历史讨论文字残留，非文档正文）→ C5 **全面解决**（文档术语与代码字段名 `retryOn` 对齐）。

### 🟢 C6. `docs/04-configuration.md:39` `stickyTtl` 字段文档领先（低·文档领先）

- 文档位置：`docs/04-configuration.md:39`（"stickyTtl 仅 iphash 策略生效：会话保持时长（秒）"）。
- 代码真相：`src/config/schema.js:1030` 的 `OriginPool` 校验仅含 `strategy` 枚举，无 `stickyTtl` 字段；contracts `OriginPool` typedef 也无。文档描述了代码未实现的字段。
- 方向：文档领先（代码无此字段）
- 是否需确认：否，删除或标注"规划中"。

### 🟢 C7. `docs/02-prerequisites.md` 内部 Node 版本矛盾（低·文档内部不一致）

- 文档位置：`docs/02-prerequisites.md:12` 要求 "Node.js ≥ 22"，但第 34 行 nvm 示例 `nvm install 20` / `nvm use 20`。
- 代码真相：edgeone.json `NODE_VERSION=22`、README 要求 ≥22、GitHub Actions `node-version:'22'` 一致；02 的 20 示例自相矛盾。
- 方向：都错（内部不一致）
- 是否需确认：否，统一为 22。

### 🟢 C8. "三步构建"措辞滞后（低·文档落后）

- 文档位置：`docs/11-architecture.md:290`、`README.md:92`、`docs/02-prerequisites.md:127` 称"三步构建"（内联兜底+dist/public+打包），实际 `build.mjs` 为四步（多"产物自检" `verify`）。
- 代码真相：`build.mjs` 日志 `[1/4]~[4/4]`，02 的步骤 5 示例日志已正确，但"三步"计数措辞仍残留。
- 方向：文档落后
- 是否需确认：否，统一"四步构建"。

### 🟢 C9. `docs/10-api-reference.md:16` 未说明 `/auth/me` 免登录（低·文档不严谨）

- 文档位置：`docs/10-api-reference.md:16`（"除 POST /auth/login 和 POST /auth/logout 外，所有接口都需要登录"）。
- 代码真相：`src/api/router.js:57-60` 的 `GET /auth/me` 声明 `auth:false` 也免登录。
- 方向：文档轻微落后
- 是否需确认：否，补一句 `/auth/me` 免登录。

---

## D、流量序列 UI（源文件层 vs 产物层）

> 关键前提：D 类所有"源文件层已正确"的判断，都依赖 **A1 闭合（重新 build 产物）** 才对线上生效。在 A1 未解决前，线上跑的是旧产物，U 类现象仍可能出现。

### 🟢 D1. 阶段编号/术语漂移：前端"④ 匹配规则""③ 源站"与 docs/12 体系冲突（中·一致性）—— **已修复（源码层）**

- 位置：`web/app.js:1425`（`④.7 候选源站`）、`:1948`、`:1972`（`流量序列 → ④ 匹配规则`）、`:1847`（`openSiteDrawer` 提示"② 安全 / ③ 源站 / ④ 规则"）。
- 现象：`docs/12-request-flow.md:290-322` 的「18 阶段映射表」规定 ③=首要分流、④=URL 规范化、⑤~⑯=规则阶段、⑭=回源循环（源站池）、②=安全。但 `web/app.js` 散落的注释/提示文案把"规则"统称"④ 匹配规则"、把"源站"称为"③"、候选源站称"④.7"，与映射表/主 UI 渲染错位（用户会在文档里找不到对应阶段）。
- **二次全面复核（2026-08-11 23:43，确认问题真实存在，非误判，并澄清根因）**：
  1. 实读 `web/app.js` 主 UI 渲染段（行 528-607、768）：流量序列主图**已正确**采用 18 阶段体系（`③ 本次回源对象（推导·只读）`、`④ URL 规范化`、`⑨ Origin Rules 含候选源站`、`⑭ 回源循环`），与 docs/12 映射表一致——**主渲染无漂移**。
  2. 漂移仅存在于 `web/app.js` 的**注释 + 散落提示文案**（部分渲染到用户界面，如 :1847 hint、:1948/:1972 模板预览、:2255/:2472 抽屉说明），共 11 处：`:828`「④ 规则引擎」、`:1413`「④.1/④.2 通用抽屉」、`:1425`「④.7 候选源站」、`:1723`「④.7 候选源站」、`:1847`「源站/规则/安全分别在 ③/④/②」、`:1948/:1972`「④ 匹配规则」、`:2051`「②/④/⑧」、`:2158`「④.5/④.8/⑧.1」、`:2255`「③ 这包…④ 路由规则、⑧ 源站池」、`:2472`「路由规则（④）与源站池（⑧）」。
  3. 根因澄清：`docs/12-request-flow.md` 自身**序章流程图（行 64-156）与「18 阶段映射表」（行 290-320）编号体系冲突**——序章里 ④=匹配规则、④.7=候选源站；映射表里 ④=URL 规范化、候选源站拆入 ⑨/⑭。前端散落文案采用的是序章流程图细分，与映射表/主 UI 错位。D1 判定"前端与 docs/12 冲突"真实成立，但**主 UI 渲染未错位**，仅零散文案漂移。
- 类型：文档-代码一致性（阶段术语漂移，范围限于注释/提示文案）
- 方向：都错（前端零散文案与 docs/12 映射表/主 UI 体系不一致）
- 是否需确认：否。
- **修复（2026-08-11 23:43，源码层 `web/app.js`）**：以 docs/12 映射表 + 主 UI 渲染为权威，把 11 处漂移文案统一到 18 阶段：
  - 规则统称由「④ 匹配规则 / ④ 规则引擎 / ④ 路由规则」→「⑤~⑯ 规则 / 规则（⑤~⑯）」；
  - 「④.7 候选源站 / ④.1/④.2 通用抽屉 / ④.5/④.8」→「⑨ Origin Rules（候选源站）/ 通用抽屉（⑤~⑯）/ ⑨/⑪/⑭」；
  - 「源站（③）/源站池（⑧）」→「源站（③ 首要分流 / ⑭ 源站池）/ ⑭ 源站池」；
  - `:1847` hint、`:2255` frag-note、`:2472` 抽屉说明等用户可见文案同步对齐。
  - **未改动**主 UI 渲染段（行 528-607 等已正确，避免引入回归）。
- **修复后二次验证（源码层）**：`search_content` 在 `web/app.js` 再搜 `④ 匹配规则|④.7|④.1|④.2|④.5|④.8|路由规则（④）|源站池（⑧）|②/④/⑧|③ 源站` → **0 命中**（仅 mdlist/docs 讨论文字残留）；主 UI 渲染段 `③ 本次回源对象`/`④ URL 规范化`/`⑨ Origin Rules`/`⑭ 回源循环` 仍在，未被误改。
- **生效前提（与 A1/D2 一致）**：本修复仅改 `web/app.js` 源文件，需重新 `npm run build`（A1 方案 Y，`minify:false` 保活间接引用）才能进入 `dist/public/assets/app.js` 与 `_worker.js`；当前产物仍为旧文案，待 A1 重建后一并生效。`docs/12-request-flow.md` 序章流程图与映射表的内部编号冲突（④ 双义）建议另立文档项清理，超出 D1 文案对齐范围。

### 🟢 D2. 源文件层 U1-U5 已正确（复核结论，非新问题）

- 审查员独立核查 `web/app.js` 源文件：U1（抽屉越界，⑤⑥⑦⑧⑨⑪⑯ 均传 `match`）、U2（⑨ 平铺，match 覆盖 origin 类）、U3（⑫ 独立 `openCacheGenDrawer` 解耦 ①）、U4（⑭ 源站级规则引擎已移除，仅留物理 Host 字段）、U5（③ 只读 toast，owner=null）在源码层**均已正确**。
- **但**：上述修复**仅存在于 `web/app.js` 源文件**，因 A1（未重新 build）而**未进入任何产物**，线上不生效。故 D2 不构成独立待改项，而是被 A1 吸收。
- 方向：—
- 是否需确认：随 A1。

---

## 三、汇总表

| 编号 | 严重级 | 类型 | 方向 | 是否需确认 |
|---|---|---|---|---|
| A1 | 高 | 构建产物过期 / 未重新 build | — | **是（P0）** |
| B1 | 中 | 逻辑空挂（误删源站风险） | — | 是 |
| B2 | 中 | 可用性风险（signedUrl 空 secret） | — | **否（误判/已闭环）** |
| B3 | 低 | 死代码 | — | 否 |
| B4 | 低 | 未用公共 API | — | 否 |
| B5 | 低 | 逻辑冗杂（hourKey 三处） | — | 否 |
| B6 | 低 | 轻微冗余 | — | 否 |
| B7 | 低 | 注释误导 | — | 否 |
| 🟢 C1 | 中 | 文档落后（target 漏 origin） | 代码领先 | 否（已修复） |
| 🟢 C2 | 中 | 文档领先（proxy/image.js 不存在） | 文档领先 | 否（已修复） |
| 🟢 C3 | 中 | 文档与 toml 矛盾（sockets flag） | 文档领先 | 否（已修复） |
| 🟢 C4 | 中 | 文档内部矛盾（asn/country/scheme 旧名） | 都错 | 否（已修复） |
| 🟢 C5 | 中 | 文档落后（fallbackStatusCodes 旧名） | 代码领先 | 否（已修复） |
| C6 | 低 | 文档领先（stickyTtl） | 文档领先 | 否 |
| C7 | 低 | 文档内部不一致（Node 22/20） | 都错 | 否 |
| C8 | 低 | 文档落后（三步构建） | 文档落后 | 否 |
| C9 | 低 | 文档不严谨（/auth/me 免登录） | 文档落后 | 否 |
| 🟢 D1 | 中 | 阶段术语漂移（前端零散文案 vs docs/12 映射表） | 都错 | 否（已修复·源码层，待 build 生效） |
| D2 | — | 源文件 U1-U5 已正确（被 A1 吸收） | — | 随 A1 |

---

## 四、建议讨论顺序（待确认后决定怎么改）

1. **A1（P0）**：先确认实际部署形态是否依赖 `src/ui.gen.js` / `dist/public/assets/app.js`，并重新 `npm run build` 生成产物、比对符号，确认流量序列 UI 进入线上。这是其它 UI 类问题（D1/D2）生效的前提。
2. **B1**：`pools.collectRefs` catch 置 `truncated=true`，防止误删源站。
3. **B2 / C 类文档**：多为文档对齐代码或低风险校验，可批量处理。
4. **B3-B7、C6-C9**：清理/措辞类，优先级最低。
